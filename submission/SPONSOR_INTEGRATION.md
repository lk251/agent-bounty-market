# Sponsor Integration

This table is intentionally conservative. The winning bundle is
`Mixed real/fallback`: it packages real evidence where available, deterministic
fallbacks where live credentials or runtime are absent, and exact paths to turn
each blocked row live.

| Sponsor | Implemented | Recorded-real | Fallback | Blocked | Why structural | Exact path to live |
| --- | --- | --- | --- | --- | --- | --- |
| Stripe | Checkout funding, signed webhook ingestion, internal ledger credit, Connect Transfer adapter, split allocation, reconciliation, status/runbook tooling | Prior sandbox full-transfer IDs are recorded and referenced from the bundle | Winning bundle uses deterministic fake external transfer for the split path | Fresh split Connect Transfer is blocked without loaded sandbox key, CLI webhook secret, and connected account | The trusted boundary already separates internal credit, external transfer, webhook verification, idempotency, and reconciliation | Load test env, run `stripe listen`, create Checkout, process signed event, run accepted verifier, attach test account, run `stripe-release-transfer`, then import the fragment |
| GitHub | Digest-bound bounty contract markers, claim markers, PR submission markers, signed webhook ingestion, fake client, publication journal | None in this bundle | Fake GitHub client shows issue, claim, PR, and result lifecycle deterministically | Real GitHub path is blocked without integration flag, token, repository, and webhook secret | The contract and webhook schemas are already the durable interface; real transport swaps into the same tables | Configure a fine-grained development token/webhook, run `github-status`, run `demo-github-motoko-live`, then import the real fragment |
| Hermes | Local Hermes executable/version evidence, project/solver skill manifests, runtime wrappers, fake project/solver agent decisions | Hermes executable is recorded as real local evidence | Project-agent and solver-agent decisions use deterministic fake runtimes | Nemotron-backed Hermes decisions are blocked without reviewed wrapper commands and provider credentials | Agent decisions already pass through schema validation, policy gates, safe trace digests, and fake/real truth labels | Configure reviewed project and solver wrapper commands, provider env, run `demo-hermes-decisions --require-real`, then import the fragment |
| NVIDIA/OpenShell | Safe status probes, policy/manifest digests, OpenShell/NemoClaw backend boundary, sandbox command path | None in this bundle | Local isolated verifier remains the fallback execution boundary | Docker/OpenShell/NemoClaw runtime is unavailable in this environment | The verifier already records backend, policy digest, and blocker; the live backend is an execution-boundary replacement, not a payment-rule rewrite | Install/approve Docker + OpenShell/NemoClaw runtime, run `nvidia-runtime-status`, run `demo-nvidia-sandbox --require-real`, then import the fragment |
