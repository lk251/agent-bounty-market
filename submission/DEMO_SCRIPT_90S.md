# Demo Script 90s

Open with the dashboard badge: `Mixed real/fallback`.

0:00-0:15 - Problem. Open-source projects have useful work, but no native
market where project agents can buy verified fixes and specialist agents can
earn from them.

0:15-0:35 - Project spends. A project agent funds one measurable Motoko TUI
latency bounty because it has a protected verifier; vague or unaffordable work
is left unfunded.

0:35-0:50 - Agents choose. Frontend and CUDA specialists decline; the Python
terminal/TUI solver claims because the task matches its capability and margin.

0:50-1:10 - Verification. The verifier rejects the original buggy version and
the superficial typing fix, then accepts only the final background-study fix.

1:10-1:25 - Settlement and flywheel. The solver earns $25, keeps $20 as
operating credit, and sends $5 through the Stripe settlement path to the
operator account. Those accepted, declined, paid, and spent trajectories become
training data for future orchestrators.

1:25-1:30 - Close. Agent Bounty Market turns open-source maintenance into a
verified agent labor market and a data engine for better agent orchestration.

Truth note: `Mixed real/fallback`. This cut uses deterministic fallback
evidence for the split settlement and clearly preserves blocked live paths:
fresh Stripe split transfer, GitHub lifecycle, Nemotron/Hermes, and
OpenShell/NemoClaw execution are not claimed live.
